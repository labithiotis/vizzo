pre-commit:
  commands:
    check:
      run: bun run check

commit-msg:
  commands:
    conventional:
      run: 'grep -Eq "^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\\([^)]+\\))?!?: .+" {1}'
      fail_text: "Use Conventional Commits: type(scope): subject"
